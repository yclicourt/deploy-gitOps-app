apiVersion: v1
kind: Secret
metadata:
  name: {{.Values.backend_tasks_app.secretName}}
  namespace: {{.Values.backend_tasks_app.namespace}}
type: Opaque
stringData:
  DB_NAME: tasksdb
  DB_USER: postgres
  DB_PASSWORD: seguro12345
  DB_HOST: tasks-db-svc 
  DB_PORT: '5432'
  CORS_ALLOWED_ORIGINS: "http://tasks-app.local,http://localhost,http://127.0.0.1"
  ALLOWED_HOSTS: "http://tasks-app.local,http://localhost,http://127.0.0.1"